#include<stdio.h>
#include<string.h>
int main()
{
    char str[20];
    printf("Enter your name \n");
    fgets(str,20,stdin);
    strupr(str);
    printf("convert into uppercase %s",strupr(str));
    return 0;
}
