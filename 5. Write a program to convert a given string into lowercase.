#include<stdio.h>
#include<string.h>
int main()
{
    char str[20];
    printf("Enter your name \n");
    fgets(str,20,stdin);
    strlwr(str);
    printf("convert into lowercase %s",strlwr(str));
    return 0;
}
